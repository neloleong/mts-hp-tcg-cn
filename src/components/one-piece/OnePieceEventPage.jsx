export default function OnePieceEventPage() {
  return (
    <main className="op-page">
      <section className="op-hero">
        <p className="op-kicker">ONE PIECE CARD GAME</p>
        <h1>Events</h1>
        <p>This section will collect tournaments, store events and player meetups.</p>
      </section>

      <section className="op-empty-state">Events page is under preparation.</section>
    </main>
  );
}
